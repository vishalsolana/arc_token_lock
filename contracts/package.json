{
  "name": "contracts",
  "version": "1.0.0",
  "description": "TokenLocker contract for Arc chain",
  "main": "index.js",
  "scripts": {
    "compile": "node scripts/compile.js",
    "test": "node scripts/compile.js && hardhat test --no-compile",
    "deploy:testnet": "node scripts/compile.js && hardhat run scripts/deploy.js --network arcTestnet",
    "deploy:mainnet": "node scripts/compile.js && hardhat run scripts/deploy.js --network arcMainnet"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.6.1",
    "hardhat": "^2.29.1",
    "solc": "^0.8.24"
  }
}
